server:
  port: 10001
spring:
  application:
    name: admin
  profiles:
    active: dev

eureka:
  instance:
    instance-id:  ${spring.application.name}:${server.port}
    prefer-ip-address: true     #访问路径可以显示IP地址
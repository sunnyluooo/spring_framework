server:
  port: 9001
spring:
  application:
    name: gateway
  profiles:
    active: dev
  cloud:
    gateway:
      routes:
       - id: admin
         uri: lb://admin
         predicates:
           - Path=/admin/**

